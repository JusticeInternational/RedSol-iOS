# fill withgraphql queries

mutation logInUser($username: String!, $password: String!){
  users{
	logIn(username: $username, password: $password){
	  sessionToken
	}
  }
}

# will add mutation for register
